# Frontend

Dashboard inicial del proyecto Smart Air & Noise Monitor.

## Ejecución rápida

Sirve esta carpeta con cualquier servidor estático, por ejemplo:

```bash
cd frontend
python3 -m http.server 3000
```

Después abre `http://localhost:3000`.

El frontend consume el backend en `http://localhost:8000/api/v1/dashboard`.
